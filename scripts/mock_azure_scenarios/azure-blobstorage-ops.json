{
  "skill": "azure-blobstorage-ops",
  "scenarios": [
    {
      "name": "normal_operation",
      "description": "正常 Blob Storage 操作场景：创建账户 → 容器列表 → Blob 列表 → 删除",
      "commands": [
        "az storage account create --name teststorage --resource-group test-rg --sku Standard_LRS",
        "az storage container list --account-name teststorage",
        "az storage blob list --container-name test-container --account-name teststorage",
        "az storage account delete --name teststorage --resource-group test-rg --yes"
      ],
      "expected": "success"
    },
    {
      "name": "partial_failure",
      "description": "部分失败场景：存储账户创建成功但创建容器失败（名称冲突）",
      "commands": [
        "az storage account create --name failstorage --resource-group test-rg --sku Standard_LRS",
        "az storage container create --name existing-container --account-name failstorage"
      ],
      "expected": "partial_fail",
      "fail_at": [1]
    },
    {
      "name": "full_failure",
      "description": "完全失败场景：存储账户不存在",
      "commands": [
        "az storage account show --name nonexistentstorage"
      ],
      "expected": "full_fail"
    }
  ]
}
