{
  "skill": "azure-queue-storage-ops",
  "operations": {
    "queue_create": {
      "health_check": {
        "api": "az storage queue show",
        "args_template": ["storage", "queue", "show", "--name", "{{queue_name}}", "--account-name", "{{account_name}}", "--output", "json"],
        "parse_field": "name",
        "expected": "{{queue_name}}"
      },
      "healing_rules": [],
      "_reason": "provisioning wait handled by health_check re-observation; queue exists check only",
      "escalate_on": [],
      "risky": false
    },
    "queue_delete": {
      "risky": true,
      "healing_rules": [],
      "escalate_on": []
    }
  }
}
