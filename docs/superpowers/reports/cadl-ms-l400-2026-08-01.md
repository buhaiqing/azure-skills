## [CADL-Review] MS Level 400 Capable — 2026-08-01

### 可复用资产
- Risk tier R0/R1/R2 as data (`scripts/risk_tiers.json`) + `apply_tier_gates()` → 落点: `scripts/risk_tiers.py`；auto_feedback_loop 消费
- Live canary contract (8 read-only observes) separate from mock suite → 落点: `scripts/live_canary.py`
- MS L400 vs Gartner L4 措辞纪律 → 落点: baseline + README 双语文案

### Anti-Pattern 发现
- JSON 场景文件写 `#` 注释 → 根因: 当 markdown 写 → 避免方式: 纯 JSON，说明放 `description` 字段
- 用 Gartner L4 认证冒充 Microsoft Level 400 → 避免方式: 对外两套 claim 分栏
