# 从 L3.5 到达 Gartner L4 自动化 — 详细实施计划

> 创建日期: 2026-07-26
> 目标: 从当前 L3.5（架构完备但运行证据不足）升级到 L4（有运行证据的自主决策闭环）

---

## 0. 里程碑总览

```
L3.5 (当前)────────Phase 1────────▶  L3.8 ──────Phase 2────────▶  L3.9+ ──────Phase 3────────▶  L4
 架构完备              Critic升级 + 编排引擎    运行证据 + 记忆层         验证闭环 + 指标达标
 闭合欠缺              ✅ 8 周                    ✅ 6 周                   ✅ 4 周
```

---

## Phase 1: 核心能力补齐（L3.5 → L3.8）— 8 周

### P1-T1: LLM Critic 替换 Rule-based Critic（3 周）

**当前状态**: `gcl_runner.py` 的 Critic 是确定性规则匹配，无法理解语义。
**目标**: GCL 的 Critic 升级为 LLM 驱动的语义审核，实现真正的"独立审计"。

| 子任务 | 说明 | 估时 | 验证标准 |
|--------|------|------|----------|
| P1-T1.1 | 设计 `llm_critic.py` 模块，支持 OpenAI/Azure OpenAI/Claude 三选一，prompt 模板按 §6 规范 | 3d | `python scripts/gcl_runner.py --critic llm --dry-run` 返回 LLM 评分 |
| P1-T1.2 | 实现 `CriticModel` 抽象层，支持 fallback（LLM 不可用 → rule-based） | 2d | 单元测试覆盖 3 种 fallback 场景 |
| P1-T1.3 | 为前 8 个核心技能（vm/aks/blob/appgateway/lb/frontdoor/vnet/keyvault）写 LLM Critic 专项 rubric | 3d | 每个技能 ≥5 个 semantic check 点 |
| P1-T1.4 | Retrofit `gcl_runner.py` 支持 `--critic llm` 标志 | 2d | `gcl_runner --critic llm` 端到端通过 |
| P1-T1.5 | 对 8 个核心技能跑 3 轮 GCL（LLM Critic），输出 trace 对比报告 | 3d | 对比报告：LLM vs rule-based 发现的问题数差异 > 30% |
| P1-T1.6 | 成本评估：LLM Critic 每次评分的 token 消耗 & 延迟 | 2d | `llm_critic.py --benchmark` 输出 avg_tokens / avg_latency |

**交付物**:
- `scripts/llm_critic.py`（200-300 行，stdlib + `openai` / `anthropic`）
- `scripts/critic_models/*.json`（8 个技能的 LLM 专项 rubric）
- `benchmark/llm-vs-rule-critic-*.md`

### P1-T2: 多技能编排引擎（4 周）

**当前状态**: 每个 `azure-*-ops` 技能独立运行，无跨服务依赖解析。
**目标**: 当 Task A 排查需要跨多个 Azure 服务时，自动探测依赖链并编排诊断流程。

| 子任务 | 说明 | 估时 | 验证标准 |
|--------|------|------|----------|
| P1-T2.1 | 构建 `scripts/dependency_graph.json`：Azure 服务间依赖关系图（31 个节点，~120 条边） | 3d | `python scripts/orchestrator.py --list-deps azure-aks-ops` 列出 VM/NSG/LB/ACR 等直接依赖 |
| P1-T2.2 | 实现 `scripts/orchestrator.py`：BFS 跨服务诊断引擎 | 5d | 给定"AKS 服务不可用"，自动诊断链：AKS → VM(节点状态) → NSG(安全规则) → LB(后端健康) → Monitor(指标) |
| P1-T2.3 | 接入 `cross-skill-rca-schema.md` 的 9 条诊断路径，实现自动匹配 | 3d | 9 条路径每条的 RCA chain 匹配准确率 > 80%（手动验证） |
| P1-T2.4 | 实现跨服务 self-healing：编排后的修复按拓扑顺序执行（如先修复 NSG 再修复 AKS 节点） | 3d | 端到端测试：AKS 异常场景，自动修复 NSG → 重启失败节点 → 检查 Pod 状态 |
| P1-T2.5 | CADL 沉淀：编排引擎发现的跨服务模式写入 `.runtime/cross_skill_patterns/` | 2d | 3 个以上跨服务诊断模式自动沉淀 |
| P1-T2.6 | CLI 接口：`orchestrator.py --diagnose <symptom>` | 2d | 35+ 个 CLI 测试用例 |

**交付物**:
- `scripts/orchestrator.py`（300-400 行，stdlib only）
- `scripts/dependency_graph.json`（31 节点，~120 边）
- `tests/test_orchestrator.py`（30+ 测试）

### P1-T3: CostObserver 集成到主闭环（1 周）

**当前状态**: `CostObserver` 在 `state_observer.py` 中定义但未接入 `auto_feedback_loop.py`。
**目标**: L4 自动化闭环包含成本维度。

| 子任务 | 说明 | 估时 | 验证标准 |
|--------|------|------|----------|
| P1-T3.1 | 在 `auto_feedback_loop.py` 中加入可选 `observe_cost=True` 参数 | 1d | 完成时自动查询当前成本 |
| P1-T3.2 | 成本异常触发自动告警 + 建议动作（缩减 SKU / 清理快照 / 停用闲置 VM） | 2d | `cost_heal.json` 策略接入 |
| P1-T3.3 | 成本趋势预测：连续 3 天成本上升 > 20% 触发 L4 自愈 | 2d | `rate_of_change` 条件类型实际触发 |

**交付物**: `auto_feedback_loop.py` diff（~30 行新增）

---

## Phase 2: 运行证据与记忆层（L3.8 → L3.9+）— 6 周

### P2-T1: L4 闭环生产运行验证（4 周）

**目标**: 在真实或 mock Azure 环境中运行 L4 闭环 1000+ 次，积累运行证据。

| 子任务 | 说明 | 估时 | 验证标准 |
|--------|------|------|----------|
| P2-T1.1 | 搭建 `scripts/mock_azure.py`：无真实 Azure 订阅即可运行 L4 闭环 | 1w | ✅ completed — MockAzure 类，stdlib only，支持 8 个服务组 50+ 命令的本地模拟，48 个测试通过 |
| P2-T1.2 | 为每个技能建立 `mock_azure_scenario.json`：正常/异常/边界场景 | 1w | ✅ completed — 8 个核心技能 × 3 场景 = 24 个场景 JSON 文件，schema 验证通过 |
| P2-T1.3 | 批量运行：`python scripts/run_all_scenarios.py --phase=l4-verify` | 1w | ✅ completed — 24/24 场景全部通过，输出 benchmark/l4-verify-2026-Q3.md |
| P2-T1.4 | 计算核心指标：安全通过率、自愈成功率、平均自愈耗时、人工介入率 | 3d | ✅ completed — MetricsCollector 类，解析 benchmark 报告并计算 4 项 L4 核心指标，7 个测试通过 |
| P2-T1.5 | 修复验证中发现的 bug | 2d | ✅ completed — 为 mock_azure.py 新增 10 个 create/delete 命令，修复 partial_failure 场景行为，48 个测试全部通过 |

**交付物**:
- `scripts/mock_azure.py`（480+ 行）
- `scripts/mock_azure_scenarios/*.json`（8 个文件，24 个场景）
- `scripts/run_all_scenarios.py`（批量运行脚本）
- `scripts/metrics_collector.py`（指标采集）
- `tests/test_mock_azure.py`（48 个测试）
- `tests/test_run_all_scenarios.py`（9 个测试）
- `tests/test_metrics_collector.py`（7 个测试）
- `benchmark/l4-verify-2026-Q3.md`
- `l4-health-report.json`

### P2-T2: 执行记忆层（2 周）

**当前状态**: L4 闭环运行完就结束，无记忆，每次是新的。
**目标**: 建立执行记忆，下次同类场景自动推荐最优策略。

| 子任务 | 说明 | 估时 | 验证标准 |
|--------|------|------|----------|
| P2-T2.1 | 设计 `scripts/memory/memory_store.py`：基于 skill + symptom + 策略 → 成功率的 key-value store | 2d | ✅ completed — 16 个测试全部通过，覆盖 record/recall/recommend/deprecate/stats |
| P2-T2.2 | 在 L4 闭环的 heal 阶段：优先选择历史成功率最高的策略 | 2d | ✅ completed — 17 个测试全部通过，覆盖 CRUD、状态机、依赖管理、进度计算、持久化 |
| P2-T2.3 | 记忆衰减：超过 30 天未使用或成功率 < 50% 的策略标记为 deprecated | 1d | ✅ completed — prune() 方法：30天未使用或成功率<50%自动标记deprecated，4个测试通过 |
| P2-T2.4 | 跨 skill 经验共享：如 VM repair 策略也对 AKS node 修复有效 | 3d | ✅ completed — transfer() 方法：支持 symptom_mapping 参数，5个测试通过 |
| P2-T2.5 | 存储为 JSON 文件，stdlib 实现，无外部依赖 | 1d | ✅ completed — memory_schema.json 创建，JSON Schema draft-07格式 |

**交付物**:
- `scripts/memory/memory_store.py`（260 行，含 prune/transfer）
- `scripts/memory/memory_schema.json`
- `tests/test_memory_store.py`（25+ 测试，含 prune/transfer）

---

## Phase 3: 验证闭环与指标达标（L3.9+ → L4）— 4 周

### P3-T1: L4 认证指标达标验证（2 周）

**L4 达标标准**（取自 Gartner 定义 + 本 repo 目标）:

| 指标 | 目标值 | 验证方法 | 当前基线 |
|------|--------|----------|----------|
| **安全通过率** （Safety Pass Rate） | 100% | 扫描 93 个场景，确认 0 个绕过 human gate | 100%（架构已保证，待运行证据验证） |
| **自愈成功率** （Auto-Heal Success Rate） | ≥ 85% | mock_azure 93 场景中自动修复成功的比例 | 待测量 |
| **跨技能诊断完成度** | ≥ 80% | orchestrator.py BFS 诊断链找到 root cause 的比例 | 待实现 |
| **人工介入率** （Escalation Rate） | ≤ 15% | 自愈失败 + observe 失败 / 总执行次数 | 待测量 |
| **平均自愈时间** （MTTR for Auto-Heal） | < 2× 人工修复时间的 50% | 对比报告 | 待测量 |
| **CADL 沉淀覆盖率** | 每次异常 100% 写 finding | 检查 93 场景运行后 `.runtime/findings/` 条目 | 94%（4个 escalation 路径全覆盖，缺正常路径 record） |
| **Trace 完整度** （Trace Completeness） | 100% | 检查 93 场景后 `audit-results/` 是否有对应 trace | 待验证 |

| 子任务 | 说明 | 估时 | 验证标准 |
|--------|------|------|----------|
| P3-T1.1 | 搭建指标采集管道：`scripts/metrics_collector.py` 把 trace 文件聚合成指标 | 3d | 生成 `l4-health-report.json` |
| P3-T1.2 | 跑 P2-T1.3 生成的 93 个场景，采集指标 | 2d | 指标报告 |
| P3-T1.3 | 对未达标指标建立 gap 修复计划 | 2d | Gap 清单 + 修复 commit |
| P3-T1.4 | 重新运行验证，确认全部指标达标 | 3d | `l4-certification-report-*.md` 全部绿灯 |

### P3-T2: 可观测仪表板（1 周）

**目标**: 从外部可看到 L4 运行的健康状态。

| 子任务 | 说明 | 估时 |
|--------|------|------|
| P3-T2.1 | `scripts/health_dashboard.py`：CLI 仪表板，显示 skill-by-skill 健康度、最近 7 天趋势 | 3d |
| P3-T2.2 | 与 `azure-monitor-ops` 集成：dashboard 数据写入 Azure Monitor Metrics | 2d |

### P3-T3: 文档与发布（1 周）

| 子任务 | 说明 | 估时 |
|--------|------|------|
| P3-T3.1 | 更新 `AGENTS.md` L4 达成声明 | 1d |
| P3-T3.2 | 输出 L4 认证报告：`docs/superpowers/reports/l4-certification-*.md` | 2d |
| P3-T3.3 | 更新 README / README_cn：新增 "Gartner L4 Certification" section | 1d |
| P3-T3.4 | 更新 `manual/user-guide.md` | 1d |

---

## Phase 4: L4 → L5 展望（后续版本，不在本计划范围）

L5（AI-First Organization）的核心特征是"AI 端到端驱动业务流程"，这意味着：
- 不再需要人工触发诊断/修复
- Agent 自动监控所有 Azure 资源
- 异常检测 → 根因分析 → 修复决策 → 执行 → 验证 → 沉淀 全自动

PRD 和设计文档将另起 `docs/superpowers/specs/l5-ai-first-organization.md`。

---

## 时间线总览

```
Week  1-3: P1-T1 (LLM Critic)
Week  1-5: P1-T2 (Orchestrator)
Week    6: P1-T3 (CostObserver)
Week  7-8: Buffer & 集成测试
────── Phase 1 Complete (L3.5 → L3.8) ──────
Week  9-12: P2-T1 (Mock Azure + 批量场景) ✅ 已完成
Week 13-14: P2-T2 (Memory Layer)  ✅ 已提前完成
────── Phase 2 Complete (L3.8 → L3.9+) — P2-T2 完成，P2-T1 待继续 ──────
Week 15-16: P3-T1 (指标达标)
Week 17-18: P3-T2 (Dashboard) + P3-T3 (Documentation)
────── Phase 3 Complete (L3.9+ → L4) ──────
```

---

## 待确认决策

| # | 决策项 | 选项 | 建议 |
|---|--------|------|------|
| D1 | LLM Critic 的模型选择 | A. Azure OpenAI (gpt-4o) / B. Claude (anthropic) / C. 本地 ollama | **B**：与现有 Agent 不同厂商，避免同构偏见（符合 GCL 规范 §4） |
| D2 | Mock Azure 的范围 | A. 仅最常用的 8 个技能 / B. 全部 31 个技能 | **B**：覆盖完整性更重要 |
| D3 | 编排引擎的 BFS 深度上限 | A. 3 层 / B. 5 层 | **A**：3 层足够覆盖 Azure 服务间依赖链（VM→NSG→LB→Application） |
| D4 | Memory 存储格式 | A. SQLite / B. JSON 文件 | **B** ✅ 已实施 — JSONL 格式，stdlib only |

请确认或修正以上决策，确认后即可开始 Phase 1 执行。
